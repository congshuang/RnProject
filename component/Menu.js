import React from 'react';import {    AppRegistry,    Text,    View,    StyleSheet,    Image,    Button,    ScrollView,}  from 'react-native';import {    DrawerNavigator ,    DrawerItems ,} from 'react-navigation';import Utils from '../Utils/util';import TopShow from './TopShow';import App from './App';import Login from './Login';class MyNotificationsScreen extends React.Component {    static navigationOptions = {        //{ focused: boolean, tintColor: string }        drawerLabel: 'Notifications',        //{ focused: boolean, tintColor: string }        drawerIcon: ({ tintColor }) => (            <Image                source={require('../state/spbg.png')}                style={[styles.icon, {tintColor: tintColor}]}            />        ),    };    render() {        return (            <Button                onPress={() => this.props.navigation.navigate('DrawerOpen')}                title=" MyHomeScreen ----> open drawer2"            />        );    }}const MyFirstProject = DrawerNavigator({    Notifications: {        screen: MyNotificationsScreen,    },},{    drawerWidth: 200, // 抽屉宽    drawerPosition: 'left', // 抽屉在左边还是右边    // contentComponent: CustomDrawerContentComponent,  // 自定义抽屉组件    contentOptions: {        initialRouteName: 'Notifications', // 默认页面组件        activeItemKey : 'Notifications',        labelStyle : {//标签样式            fontSize:18        },        activeTintColor: '#fff',  // 选中文字颜色        activeBackgroundColor: '#196be6', // 选中背景颜色        inactiveTintColor: '#666',  // 未选中文字颜色        inactiveBackgroundColor: '#fff', // 未选中背景颜色        style: {  // 样式            marginVertical: 0,        },        //没有作用        onItemPress : (route) => {            console.log('-------->' + JSON.stringify(route))        },    },    contentComponent: props => {        return (            <ScrollView style={styles.contenter}>                <View>                    <TopShow/>                    <DrawerItems {...props} />                </View>            </ScrollView>        )    },});const styles = StyleSheet.create({    icon: {        width: 24,        height: 24,    },    contenter:{        flex:1,    },});module.exports = MyFirstProject;